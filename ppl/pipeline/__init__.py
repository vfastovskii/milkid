"""Pipeline orchestration for the Multi-Instance Learning Kit (MILK).

Import from the specific submodules (e.g. ``ppl.pipeline.orchestrator``,
``ppl.pipeline.builder``, ``ppl.pipeline.mlflow_utils``) rather than the package
root — keeping this ``__init__`` empty avoids import cycles.
"""
